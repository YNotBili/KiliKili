package rj.kilikili.ui.theme

import androidx.compose.ui.graphics.Color

// Bilibili 品牌色
val BiliPink = Color(0xFFFB8799)  // B站品牌粉，用于点赞/投币/收藏激活态
val BiliBlue = Color(0xFF00A1D6)  // B站品牌蓝，可选用于链接等

// Wear 暗色模式容器色（参考 Orbit 颜色方案）
val WearDarkSurface = Color(0xFF000000)              // 纯黑背景（OLED 省电）
val WearDarkOnSurface = Color(0xFFE2E2E9)             // 浅灰白，保证可读性
val WearDarkSurfaceContainerLow = Color(0xFF141618)   // 低层容器
val WearDarkSurfaceContainer = Color(0xFF1D2024)      // 卡片/按钮容器
val WearDarkSurfaceContainerHigh = Color(0xFF282A2F)  // 高一层容器
val WearDarkSurfaceContainerHighest = Color(0xFF303237) // 最高层容器