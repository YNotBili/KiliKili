# KiliKili 性能 + 安全审计报告

**审计日期:** 2026-07-02
**分支:** `rewrite`(commit `e82e7a0b` + ~1000 行未提交改动)
**代码体量:** `app/` 607 个 kt 文件 + `BiliWebApi/` 162 个 kt 文件,约 13.4 万行 Kotlin
**审计方法:** 6 个并行 explorer + 1 个 adversarial verifier,共核查 13 个关键引用(12 PASS / 1 PARTIAL),主要 P0/P1 发现均经我方二次 Read 确认

---

## 1. 执行摘要(Executive Summary)

本次审计发现 **1 个 P0 安全漏洞、9 个 P1 性能/安全/正确性问题、7 个 P2 风险、若干 P3/INFO**。其中:

- **P0-1**: 全 App TLS 信任绕过,任何 MITM 攻击者可解密所有 API 流量(登录、cookie、appkey 签名)
- **P1 集群**: 播放器 250ms 不间断轮询循环 + SurfaceHolder 回调累积 + DanmakuView 永不释放;这三条在 uncommitted 改动中**没有任何改善**,且 +153 行的 PlayerViewModel 让 VM 进一步膨胀
- **P1 数据**: 备份配置空 + cookies/Room DB 明文落盘 + `EncryptedSharedPreferences` 全项目 0 引用 → 用户 cookie 进入云备份
- **P1 网络**: `runBlocking` 在 OkHttp 拦截器内拉网络请求、3 处绕 OkHttp 直连

**6 个旧 GitHub issue 状态:**
| # | 主题 | 旧优先级 | 当前状态 |
|---|---|---|---|
| 1 | 主线程 runBlocking + WBI/Cookie 并发写 | P1 | **未修复** — `runBlocking` 仍在 OkHttp dispatcher 线程而非主线程,但仍阻塞线程池;Cookie 写入有 Mutex + `synchronized` 双锁不一致的微妙竞态 |
| 2 | 登录 accountId 错 + 错误码硬编码(死代码) | P1 | **已修复** — HdQrCodeLoginViewModel 正确用 `tokenInfo.mid`,未发现硬编码 -1015/-1016 |
| 3 | 播放器 VM 膨胀 + 资源泄漏 | P1 | **REGRESSED** — uncommitted +153 行扩大 VM 表面但未解决泄漏;DanmakuView/SurfaceHolder/Surface 三个泄漏原封未动 |
| 4 | 下载 `!!` + FileProvider 安全性 | P1 | **已修复** — `data/download/` 0 个 `!!`,`FileProvider.getUriForFile` 0 个调用点;FileProvider 是死组件 |
| 5 | 调试日志泄露 + 绕 OkHttp 图片下载 | P2 | **REGRESSED** — cookie 值 / WBI keys 明文 log 仍存在;图片下载/弹幕下载仍用 `HttpURLConnection`/`URL.openStream` 绕 OkHttp |
| 6 | 死代码 + 杂项 | P2 | **PARTIALLY_FIXED** — 19 个新文件里 17 个有 caller,`CommonRepository.kt` + `UploadRepository.kt` 仍无 caller;`ArticleRepository` 标记 `@Deprecated` 但仍在被调 |

---

## 2. 重要发现(P1)

### 🔴 P1-1: 播放器 250ms 轮询循环,无生命周期感知

**位置:** `app/src/main/java/rj/kilikili/ui/screens/player/PlayerScreen.kt:357-370`

```kotlin
LaunchedEffect(viewModel.ijkPlayer) {
    while (true) {
        currentPosition = viewModel.ijkPlayer.currentPosition   // 4 Hz 写
        duration = viewModel.ijkPlayer.duration.coerceAtLeast(0L)
        val actuallyPlaying = viewModel.ijkPlayer.isPlaying
        if (actuallyPlaying != isPlaying) { isPlaying = actuallyPlaying }
        delay(250)
    }
}
```

**影响:**
- 4 Hz 持续轮询,每次给 `currentPosition` 赋值都触发整个 PlayerScreen 重新组合(因为 `LaunchedEffect(currentPosition)` 在 1330/1501 行也消费它)
- Wear OS 屏幕 + 250ms 唤醒,JNI bridge 到 IjkMediaPlayer 持续被戳
- 与下面 P1-2 的 100ms 轮询**叠加** = ~5 次/秒的 JNI 唤醒
- `LaunchedEffect` 绑在 composition,屏幕离开 composition 时会取消,但**永远不会暂停**(Wear OS 抬手/放手时也不暂停)

**修复建议:**
- 改为 `viewModel.positionFlow` (基于 `while(isActive)` + `flow { while { emit; delay(100) } }.stateIn(scope, SharingStarted.WhileSubscribed(5000), 0L)`),UI 用 `collectAsStateWithLifecycle` 消费
- 屏幕层只在 second 变化时更新:`if (newPos / 1000 != currentPos / 1000) currentPosition = newPos`
- 抬手/ambient 模式用 `LocalLifecycleOwner.current.lifecycle.currentState == STARTED` 守卫

---

### 🔴 P1-2: 100ms 超时轮询与 250ms 轮询叠加在 PlayerScreen

**位置:** `PlayerScreen.kt:292-309`(`withTimeoutOrNull(30_000L)` 内 100ms 轮询)+ 上面 P1-1

播放器 30 秒内同时跑两个 `while(true)` 轮询 — 一个 100ms 检测历史进度应用,一个 250ms 更新 UI。叠加后 4-10 Hz 戳 ijkplayer JNI。

---

### 🔴 P1-3: SurfaceHolder.Callback 累积泄漏

**位置:** `PlayerScreen.kt:824-856`

```kotlin
AndroidView(
    factory = { ctx -> SurfaceView(ctx) },
    update = { surfaceView ->
        surfaceView.holder.addCallback(object : SurfaceHolder.Callback {
            override fun surfaceCreated(holder) { viewModel.ijkPlayer.setDisplay(holder) }
            override fun surfaceChanged(holder, format, w, h) { viewModel.ijkPlayer.setDisplay(holder) }
            override fun surfaceDestroyed(holder) {}     // 空
        })
        // ↑ 每次 recomposition 都 addCallback,从不 removeCallback
    },
    modifier = Modifier.fillMaxSize()
)
```

**影响:**
- Compose `AndroidView.update` 在每次 recomposition 时执行,每次都给 `surfaceView.holder` 添加新 callback,**从不调用 `removeCallback`**
- N 次 recomposition → N 个 callback 堆积,全部持有 `viewModel` 强引用
- `surfaceDestroyed` 空实现 + `onCleared` 没 `setDisplay(null)` → native player 持有 surface buffer 引用直至 `release()`

**修复建议:**
```kotlin
val callback = remember {
    object : SurfaceHolder.Callback {
        override fun surfaceCreated(h: SurfaceHolder) { viewModel.ijkPlayer.setDisplay(h) }
        override fun surfaceChanged(h: SurfaceHolder, fmt: Int, w: Int, h: Int) { viewModel.ijkPlayer.setDisplay(h) }
        override fun surfaceDestroyed(h: SurfaceHolder) { viewModel.ijkPlayer.setDisplay(null) }
    }
}
DisposableEffect(surfaceView) {
    surfaceView.holder.addCallback(callback)
    onDispose { surfaceView.holder.removeCallback(callback) }
}
```

---

### 🔴 P1-4: DanmakuView 永不释放 — HandlerThread 持续泄漏

**位置:** `PlayerScreen.kt:167` + `379-383`

```kotlin
var danmakuView by remember { mutableStateOf<DanmakuView?>(null) }   // line 167

DisposableEffect(Unit) {
    onDispose {
        viewModel.reportFinalProgress(viewModel.ijkPlayer.currentPosition)   // 只汇报进度
        // ❌ 没有 danmakuView?.release()
        // ❌ 没有 danmakuView?.hide() 或 .pause()
    }
}
```

`DanmakuFlameMaster/DanmakuView.java:151-177` 的 `release()` 会退出内部 `HandlerThread` + `DrawHandler`。每次进入播放器都泄漏一个 HandlerThread + 渲染线程,直到 GC。

**修复:** onDispose 内 `danmakuView?.release(); danmakuView = null`

---

### 🔴 P1-5: `IjkMediaPlayer` 释放时未先解绑 Surface

**位置:** `app/src/main/java/rj/kilikili/ui/screens/player/PlayerViewModel.kt:641-648`

```kotlin
override fun onCleared() {
    super.onCleared()
    currentPlayTag.incrementAndGet()
    preparationCancelled.set(true)
    stopProgressReporting()
    ijkPlayer.release()            // ❌ 之前没 setDisplay(null)/setSurface(null)
}
```

**验证:** 全项目 0 个 `setDisplay(null)`/`setSurface(null)` 调用

native player 在 `release()` 之前仍持有 surface buffer 引用,可能导致 native 层 SurfaceTexture 回收异常。

**修复:** `ijkPlayer.setDisplay(null); ijkPlayer.setSurface(null); ijkPlayer.release()`

---

### 🔴 P1-6: `allowBackup=true` + 空备份规则 → 全部进云备份

**位置:** `app/src/main/AndroidManifest.xml:27-29` + `app/src/main/res/xml/backup_rules.xml` + `data_extraction_rules.xml`

```xml
<application
    android:allowBackup="true"           <!-- ✓ enabled -->
    android:dataExtractionRules="@xml/data_extraction_rules"
    android:fullBackupContent="@xml/backup_rules">
```

```xml
<!-- backup_rules.xml: 只有注释,没有任何 <exclude> -->
<full-backup-content>
    <!-- <include .../> <exclude .../> -->
</full-backup-content>
```

```xml
<!-- data_extraction_rules.xml: cloud-backup 也是空 -->
<data-extraction-rules>
    <cloud-backup>
        <!-- 全注释 -->
    </cloud-backup>
</data-extraction-rules>
```

**影响:**
- Room DB 包含 cookie / DedeUserID / buvid3/4 / wbi 缓存 / 账号信息 → 全部进入 Google Drive 自动备份
- 任何能登录该 Google 账号的设备都能恢复完整登录态
- `androidx.security` 全项目 0 引用,`EncryptedSharedPreferences` 0 引用

**修复建议:**
1. `android:allowBackup="false"`,或
2. 备份规则里 `<exclude domain="database" path="kilikili-db"/>` + `<exclude domain="sharedpref" path="auth_prefs"/>`
3. 长期方案:用 `EncryptedSharedPreferences`/`EncryptedFile` 存敏感字段

---

### 🔴 P1-7: `runBlocking` 在 OkHttp 拦截器内做网络请求

**位置:** `BiliWebApi/src/main/java/com/huanli233/biliwebapi/httplib/BilibiliApiInterceptor.kt:119` + `WbiUtil.kt:35`

```kotlin
// BilibiliApiInterceptor.kt:118-130
if ((cookies.any { it.name == "buvid3" } && cookies.any { it.name == "buvid4" }).not()) {
    runBlocking {                                  // 阻塞 OkHttp dispatcher 线程
        Buvids.generate(biliWebApi)                 // 内部发起网络请求
    }.data?.let { ... }
}
```

**影响:**
- 第一次请求触发 buvid3/buvid4 生成时,`runBlocking` 阻塞当前 dispatcher 线程一次完整网络 RTT
- WBI 缓存失效时同样阻塞
- OkHttp 默认 dispatcher 池 64 个线程,冷启动并发请求(首页/推荐/动态同时拉)会被串行化,UI 看到首屏延迟

**修复:** 拦截器内用 `OkHttp` 异步 + `response.request.newBuilder()` 重新发起带 cookie 的请求;或者在 App 启动时预热 buvid3/4 / WBI key

---

### 🔴 P1-8: 3 处绕 OkHttp 直连

**位置:**
1. `app/src/main/java/rj/kilikili/ui/screens/image/ImageViewerScreen.kt:178-188` — 图片保存用 `URL.openConnection() as HttpURLConnection`
2. `app/src/main/java/rj/kilikili/ui/screens/player/PlayerViewModel.kt:507` — 弹幕 XML 下载用 `URL(danmakuUrl).openStream()`

```kotlin
// ImageViewerScreen.kt:178-188
val url = URL(imageUrl)
val connection = url.openConnection() as java.net.HttpURLConnection
connection.connectTimeout = 15_000
connection.readTimeout = 30_000
connection.connect()
connection.getInputStream().use { input ->
    FileOutputStream(file).use { output -> input.copyTo(output) }
}
```

**影响:**
- 不走 `BilibiliApiInterceptor` → 不带 cookie → 不带 WBI 签名 → 不带 AppSign → 走 b 站 CDN 经常 403
- 不走 `AppCookieManager` → 不维护 cookie 状态
- 没超时统一管理(OkHttp 默认 10s)、没缓存、没重试
- ImageViewer 还直接在 main scope 调,没有 dispatcher 切换(虽然协程外层是 IO)

**修复:** 改用注入的 `OkHttpClient` + `bilibiliApi` 单例,或至少在 `withContext(Dispatchers.IO)` 内用 `OkHttpClient.newCall(...).execute()`

---

### 🔴 P1-9: Cookie/WBI 明文 log(debug 构)

**位置:** `BiliWebApi/src/main/java/com/huanli233/biliwebapi/BiliWebApi.kt:112, 261-262`

```kotlin
Log.d(TAG, "    ${cookie.name}=${cookie.value} (domain: ${cookie.domain})")  // line 112
Log.d(TAG, "SUB Key: $subKey")        // line 261
Log.d(TAG, "Mixin Key: $mixinKey")    // line 262
```

虽然经 `ApiDebugSettings.isCookieLoggingEnabled()` 开关门控,但开关默认为真、debug 构建全部上 logcat。PII 风险。

**修复:** 强制脱敏(只 log name,不 log value),release 构建编译期移除

---

## 3. 中等风险(P2)

### P2-1: 26 个 `LazyColumn`/`LazyRow` 缺 `key`,Paging 刷新时全量重绑定

`grep` 统计:
- 缺 `key`:18 处(`DynamicHomeScreen.kt:123`、`UserProfileScreen.kt:236/524/615`、`CommentScreen.kt:232`、`HistoryScreen.kt:146`、`PopularScreen.kt:162` 等)
- 有 `key`:8 处(`RecommendScreen.kt:205`、`CollectionDetailScreen.kt:132` 等)
- **全部 26 个都缺 `contentType`** — AppLazyListScope 适配器给默认 `{ null }`,Compose 无法识别同类项

`DynamicHomeScreen.kt:123` 是最大风险,Paging 3 频繁插入新条目时整个可见窗口重组合。

**修复:** 给 `items()` 加 `key = item.id` 和 `contentType = "dynamic"`,参考 `RecommendScreen.kt:205-207`

---

### P2-2: `collectAsState` 143 次 vs `collectAsStateWithLifecycle` 20 次

143 个 `collectAsState(` 调用、20 个 `collectAsStateWithLifecycle(`。Wear OS 抬手/放手 lifecycle 切换频繁,`collectAsState` 在 STOPPED 状态仍继续收集,浪费 CPU/battery。

**修复:** 全项目替换为 `collectAsStateWithLifecycle`(API 来自 `androidx.lifecycle:lifecycle-runtime-compose`,应该已在依赖里)

---

### P2-3: Wear OS ambient 模式完全未实现

**位置:** 全项目 0 个 `AmbientModeSupport` / `onEnterAmbient` / `WearableActivity` 引用

**影响:** 用户在手表上放手时屏幕不会变暗、不会切到低 bit 模式、播放器不会自动暂停。Watch 电池 ~300mAh,持续全亮屏是巨大浪费。

**修复:**
1. `MainActivity` 改继承 `androidx.wear.activity.WearableActivity` 或实现 `AmbientModeSupport`
2. 实现 `onEnterAmbient` → 暂停 `ijkPlayer` + 隐藏 cover + 简化 UI
3. Manifest 加 `<uses-feature android:name="android.hardware.type.watch" android:required="true"/>` 和 `hardware.type.ambient`

---

### P2-4: Manifest 配置矛盾

**位置:** `AndroidManifest.xml:33, 35-36`

```xml
android:requestLegacyExternalStorage="true"   <!-- targetSdk=36 时无效 -->
android:usesCleartextTraffic="true"           <!-- 与 networkSecurityConfig 矛盾 -->
android:networkSecurityConfig="@xml/network_security_config"  <!-- 实际 cleartext=false -->
```

`usesCleartextTraffic="true"` 配合 `cleartextTrafficPermitted="false"` 实际以 config 优先 = 拒绝明文,但属性留着会让后续贡献者误判。

`requestLegacyExternalStorage="true"` 在 `targetSdk=36` 上**完全无效**(只对 targetSdk=29 有效),属于死代码。

**修复:** 删 `requestLegacyExternalStorage`,改 `usesCleartextTraffic="false"`

---

### P2-5: FileProvider 死组件 + file_paths.xml 死配置

**位置:** `AndroidManifest.xml:59-67` + `res/xml/file_paths.xml`

`<provider android:name="androidx.core.content.FileProvider" android:authorities="rj.kilikili.fileprovider">` 在 manifest 注册,但全项目 0 个 `FileProvider.getUriForFile` 调用点。下载走 `MediaStore`,不需要 FileProvider。

**修复:** 删 manifest provider 块 + 删 `file_paths.xml`,或者真用它来分享下载内容(下载到 `external-files-path Downloads/`,然后 `getUriForFile` 给 share intent)

---

### P2-6: 2 个孤儿仓库

`CommonRepository.kt`(整个类 11 行) + `UploadRepository.kt`(13 行) 全项目 0 caller 引用,只有自身文件被 grep 命中。

`ArticleRepository` 标记 `@Deprecated("Move to opus api")` 但 `ArticleViewModel` 仍在调用。

**修复:** 删 `CommonRepository` + `UploadRepository` + `ArticleRepository` + `ArticleViewModel`(全部一起)

---

### P2-7: `PlayerUiState` 单 `data class` 16 字段,12 处 `_uiState.copy()` → 全屏重组合

**位置:** `app/src/main/java/rj/kilikili/ui/screens/player/PlayerViewModel.kt:666-685` + 12 处 `copy()`

`PlayerUiState` 是单 `StateFlow<PlayerUiState>`,任何子字段变化(aspect ratio、loading、quality、error...)都让整个 16 字段状态对象被 `copy()` 然后 emit,PlayerScreen 整屏重组合。Compose 还把含 `List<VideoPage>` 的 data class 视为 unstable(因为 `List` 是接口),进一步放大问题。

**修复:** 拆成多个 `StateFlow<Float>`(aspect ratio)、`StateFlow<Long>`(progress)、`StateFlow<String?>`(error),给 `PlayerUiState` 加 `@Immutable`,内部 `List<>` 换 `ImmutableList<>`(kotlinx-immutable)

---

## 4. 已知偏差 / 受限项

| # | 内容 | 说明 |
|---|---|---|
| K | `DynamicCard` lambda 参数计数 | explorer 报 8,实际 7(`showFullContent: Boolean` 和 `modifier: Modifier` 不是 lambda)。底层的"lambda 在每次重组被重新分配"问题仍然成立,只是计数偏 1。verifier PARTIAL,非 fabrication |
| Image viewer download | 当前是图片保存到本地相册,不走 OkHttp 的"绕开"是**设计选择**而非 bug。优先改用注入的 `OkHttpClient` 而非 raw `HttpURLConnection` 仍是对的(否则 403 + 不带 cookie) |
| `peekBody` vs `buffer().clone().readString()` | 旧 issue #5 要求后者,实际 `peekBody` 是 OkHttp 官方推荐做法,无 bug。verifier 确认 PASS |

---

## 5. 推荐下一步(按优先级)

### 立即(本周)
1. **`allowBackup="false"`** 或写 exclude 规则
2. **修播放器三个泄漏**:DanmakuView.release、SurfaceHolder removeCallback、ijkPlayer.setDisplay(null) before release

### 本季度
3. 替换 143 个 `collectAsState` → `collectAsStateWithLifecycle`
4. LazyColumn 全部加 `key` + `contentType`
5. 拆 `PlayerUiState` 为多 flow
6. 修 ambient 模式(MainActivity 改 WearableActivity,实现 onEnterAmbient 暂停播放器)
7. 删 `CommonRepository` + `UploadRepository` + `ArticleRepository` + `ArticleViewModel`
8. 删 `FileProvider` 死组件 + `requestLegacyExternalStorage` 死属性
9. `OkHttpClient` 加 `cache()`(当前 0 缓存)+ 显式 `timeouts`
10. 修 BilibiliApiInterceptor: 拦截器内不阻塞,改异步或预热

### 长期
11. 引入 `androidx.security` 的 `EncryptedSharedPreferences` 存 cookies/tokens
12. 替换 Gson 为 kotlinx.serialization(已部分在用)消除反射开销
13. `PlayerViewModel` 拆成 `PlayerMediaViewModel` + `PlayerUiViewModel` + `DanmakuViewModel`
14. Coil 加 `.size()` / `.sizeResolver()` 给所有 cover 缩略图

---

## 6. 建议落地的 GitHub Issue(新)

| 新 issue # | 主题 | 优先级 | 一句话 |
|---|---|---|---|
| #7 | [P1] 播放器 250/100ms 双轮询 + DanmakuView 不释放 + SurfaceHolder 回调累积 + ijkplayer release 前未 setDisplay(null) | P1 | 4 条合 1 个 PR,都在 PlayerScreen.kt + PlayerViewModel.kt |
| #8 | [P1] `allowBackup=true` + 空备份规则 + 全明文存储 | P1 | cookie/wbi 缓存进云备份,androidx.security 0 引用 |
| #9 | [P1] `runBlocking` 在 OkHttp 拦截器内拉网络 | P1 | BilibiliApiInterceptor.kt:119 + WbiUtil.kt:35 |
| #10 | [P1] 3 处绕 OkHttp 直连(HttpURLConnection / URL.openStream) | P1 | ImageViewerScreen.kt:178, PlayerViewModel.kt:507 等 |
| #11 | [P2] Cookie/WBI 明文 log(debug)+ 强制脱敏 | P2 | BiliWebApi.kt:112, 261-262 |
| #12 | [P2] 26 个 LazyColumn 缺 `key`+`contentType`、143 vs 20 个 collectAsState 缺 lifecycle | P2 | 重组热点全屏 |
| #13 | [P2] Wear OS ambient 模式未实现 + 缺 uses-feature 声明 | P2 | 整个 app 在手表上不省电 |
| #14 | [P2] Manifest 配置矛盾 + FileProvider 死组件 + 2 个孤儿仓库 | P2 | cleanup |
| #15 | [P2] `PlayerUiState` 16 字段单 flow,需拆分 + 标 `@Immutable` | P2 | 替换为多 StateFlow + ImmutableList |

---

## 7. 审计方法附录

### 7.1 调研维度
| Explorer | 维度 | 主要发现数 |
|---|---|---|
| #1 | API + 网络安全 | 8(P0:1, P1:4, P2:3) |
| #2 | 播放器 + Native 资源 | 8(P1:4, P2:3, P3:1) |
| #3 | Compose 重组性能 | 13(P1:3, MEDIUM:5, LOW:4, INFO:1) |
| #4 | 数据层 + I/O | 7(P2:3, P3:4) |
| #5 | Wear OS 特定 | 6(MEDIUM:3, LOW:2, INFO:1) |
| #6 | Manifest + Intent 表面 | 6(MEDIUM:2, LOW:3, INFO:1) |
| Verifier | 13 个抽样 | 12 PASS / 1 PARTIAL / 0 FAIL |

### 7.2 防瞎编机制
- 每个 explorer 必须给 `path:line` 引用 + 实际 snippet
- verifier 对 13 个高严重度发现独立 `Read` 验证
- 我方对 P0/P1 二次 `Read` 原文确认(BilibiliApi.kt:57-80、PlayerScreen.kt:357-370/824-856/167+379-383、PlayerViewModel.kt:641-648、ImageViewerScreen.kt:178-188、AndroidManifest.xml + 全部 res/xml、BilibiliApiInterceptor.kt:119、AppSign.kt:10-11)
- 唯一偏差:`DynamicCard` lambda 计数 7(非 8),verifier 标 PARTIAL,问题成立

### 7.3 旧 issue 状态表(交叉验证)
| 旧 # | 主题 | 状态 | 证据 |
|---|---|---|---|
| 1 | 主线程 runBlocking + WBI/Cookie 并发写 | 未修复 | BilibiliApiInterceptor.kt:119 仍 `runBlocking { Buvids.generate(biliWebApi) }`;Cookie 写入 `Mutex` + `synchronized` 双锁不一致 |
| 2 | 登录 accountId 错 + 错误码硬编码 | 已修复 | HdQrCodeLoginViewModel.kt:126-131 正确用 `tokenInfo.mid`;全项目无 -1015/-1016 硬编码 |
| 3 | 播放器 VM 膨胀 + 资源泄漏 | REGRESSED | uncommitted +153 行扩大 VM 表面,DanmakuView/SurfaceHolder/Surface 三处泄漏原封不动 |
| 4 | 下载 `!!` + FileProvider | 已修复 | `data/download/` 0 个 `!!`,`FileProvider.getUriForFile` 0 调用点;FileProvider 死组件 |
| 5 | 调试日志泄露 + 绕 OkHttp 图片下载 | REGRESSED | cookie/WBI 明文 log 仍存在;ImageViewerScreen.kt:178 + PlayerViewModel.kt:507 仍绕 OkHttp |
| 6 | 死代码 + 杂项 | PARTIALLY_FIXED | 19 个新文件 17 个有 caller,CommonRepository + UploadRepository 仍无 caller;ArticleRepository `@Deprecated` 但仍被调 |
